<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>
      <% if (lang === 'pt_br') { %>
        Identifique-se
      <% } else { %>
        Identify yourself
      <% } %>
    </title>

    <style>
      body {
        background: #f8f9fa;
        font-family: Arial, Helvetica, sans-serif;
        color: #343a40;
        font-size: 17px;
      }
      body > div {
        max-width: 400px;
        margin: 20px auto;
      }
      body > div > div {
        margin-bottom: 20px;
      }
      body > div > a {
        display: block;
        padding: 14px;
        margin-bottom: 7px;
        background: #fff;
        white-space: nowrap;
        border-radius: 3px;
        box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.1);
        text-transform: none;
        text-decoration: none;
        border-width: 1px;
        border-style: solid;
        color: #fff;
      }
      .facebook {
        background: #3b5999;
        border-color: #2f4d8a;
      }
    </style>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.9/css/all.css" integrity="sha384-5SOiIsAziJl6AWe0HWRKTXlfcSHKmYV4RBF18PPJ173Kzn7jzMyFuTtk8JA7QQG1" crossorigin="anonymous">
  </head>

  <body>
    <div>
      <div>Store name</div>
      <a class="facebook" href="facebook"><i class="fab fa-facebook-square"></i> Facebook</a>
      <a href="google">Google</a>
      <a href="windowslive">Windows</a>
    </div>
  </body>
</html>
